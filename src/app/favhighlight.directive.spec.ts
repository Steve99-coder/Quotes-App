import { FavhighlightDirective } from './favhighlight.directive';

describe('FavhighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new FavhighlightDirective();
    expect(directive).toBeTruthy();
  });
});
